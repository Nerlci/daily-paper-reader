---
title: LLM-Guided Semantic Bootstrapping for Interpretable Text Classification with Tsetlin Machines
title_zh: 基于大语言模型引导语义自举的 Tsetlin 机可解释文本分类
authors: "Jiechao Gao, Rohan Kumar Yadav, Yuangang Li, Yuandong Pan, Jie Wang, Ying Liu, Michael Lepech"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12223v1"
tags: ["query:llm"]
score: 8.0
evidence: 利用LLM生成合成数据进行训练
tldr: 本研究针对预训练语言模型（PLM）不透明且计算昂贵，而符号化Tsetlin Machine（TM）缺乏语义泛化能力的问题，提出了一种LLM引导的语义引导框架。该框架通过LLM生成子意图并构建三阶段课程学习的合成数据，利用非否定TM提取可解释的语义线索并注入真实数据训练。该方法无需嵌入或运行时LLM调用，使符号模型具备了预训练语义先验，在保持高效透明的同时，实现了媲美BERT的分类性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12223v1/fig-001.webp\", \"caption\": \"Table 2: Performance of the proposed LLM-Guided TM on selected classification datasets. The bold entries indicate the highest performance, while bold and underlined indicate the second highest. A dash (–) indicates missing values not reported in literature. LLM-Guided TM results are averaged over 5 runs, and the includes standard deviation. Baseline results are drawn from literature and do not report variances, so significance testing could not be applied.\", \"page\": 7, \"index\": 1, \"width\": 929, \"height\": 276}]"
motivation: 旨在结合大语言模型的深层语义理解能力与Tsetlin Machine的可解释性，解决符号模型在文本分类中语义泛化不足的问题。
method: 利用LLM生成子意图并构建合成数据，通过非否定TM提取关键语义线索，并将其作为先验知识注入到符号模型的训练过程中。
result: 实验表明该方法在多个任务上显著提升了TM的准确率和可解释性，性能接近BERT，且无需运行时调用LLM。
conclusion: 该框架证明了可以通过知识迁移使符号模型获得强大的语义表征能力，实现了高性能、低成本且完全可解释的文本分类。
---

## 摘要
诸如 BERT 之类的预训练语言模型（PLM）提供了强大的语义表示，但成本高昂且不透明；而 Tsetlin 机（TM）等符号模型虽然具有透明性，但缺乏语义泛化能力。我们提出了一种语义自举框架，将大语言模型（LLM）的知识转化为符号形式，从而结合了可解释性与语义能力。给定类别标签，LLM 会生成子意图，通过三阶段课程（种子、核心、丰富）引导合成数据的创建，以扩展语义多样性。非否定 Tsetlin 机（NTM）从这些示例中学习，提取高置信度的文字（literals）作为可解释的语义线索。将这些线索注入真实数据，使 TM 能够将子句逻辑与 LLM 推断的语义相对齐。我们的方法不需要嵌入或运行时的 LLM 调用，却能为符号模型配备预训练的语义先验。在多个文本分类任务中，该方法相比原始 TM 提高了可解释性和准确性，在保持完全符号化和高效性的同时，实现了与 BERT 相当的性能。

## Abstract
Pretrained language models (PLMs) like BERT provide strong semantic representations but are costly and opaque, while symbolic models such as the Tsetlin Machine (TM) offer transparency but lack semantic generalization. We propose a semantic bootstrapping framework that transfers LLM knowledge into symbolic form, combining interpretability with semantic capacity. Given a class label, an LLM generates sub-intents that guide synthetic data creation through a three-stage curriculum (seed, core, enriched), expanding semantic diversity. A Non-Negated TM (NTM) learns from these examples to extract high-confidence literals as interpretable semantic cues. Injecting these cues into real data enables a TM to align clause logic with LLM-inferred semantics. Our method requires no embeddings or runtime LLM calls, yet equips symbolic models with pretrained semantic priors. Across multiple text classification tasks, it improves interpretability and accuracy over vanilla TM, achieving performance comparable to BERT while remaining fully symbolic and efficient.